# Reports

Static reviewed reports may be committed here. Per-run outputs live under the
ignored `runs/` or `reports/runs/` directories and are referenced by immutable
hash/ID rather than committed with model weights.

